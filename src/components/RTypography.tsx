import styled from "styled-components";

export const RTypography = styled("span")`
  font-size: clamp(1rem, 4vw, 1.5rem);
`;

export const BigRTypography = styled("span")`
  font-size: clamp(1.5rem, 4vw, 2rem);
`;
